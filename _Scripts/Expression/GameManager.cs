using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;

public class GameManager : MonoBehaviour {

	/* Ideias:
	 *
	 * Salvar todos Exams feitos e salvar expressões que errou.
	 * 
	 * - Practice	mode (	sem tempo, não salva estatísticas	)
	 * - Normal		mode (	com tempo, salva estatísticas		)
	 * - Exam		mode (	com tempo, salva estatísticas		) ( só pode fazer uma vez no dia ) ( Exam com todos tipos de operações ).
	 * 
	 * Toda operação possui informações próprias:
	 * - Questões que errou.
	 * - Quantidade de acertos.
	 * - Quantidade de erros.
	 * 
	 * Opção de resolução da questão.
	 * Opção para salvar questões que errar, organizando entre: "todas questões e questões por tipo (operação)"
	 * Pode apagar questões que errou.
	 *
	 */


}
